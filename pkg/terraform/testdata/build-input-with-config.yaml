config:
  backendConfig:
    key: "my.tfstate"
    storage_account_name: "mystorageaccount"
    container_name: "mycontainer"
    access_key: "myaccesskey"
actions:
  install:
    - terraform:
        description: "Install"
        input: false
  uninstall: []
  upgrade: []